#!/usr/bin/env bash
# pre-commit — roda check-all.sh (validate + lint-voz) antes de cada commit.
# Instalado via: git config core.hooksPath .git-hooks

set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
bash "$DIR/scripts/check-all.sh"
