{/* ── MAPA CLIENTES — {{NOME}} (plugin marketing-doma) ── */}
<Still id="{{NOME}}" component={MapaClientes as never}
  width={vertical.largura} height={vertical.altura} />
{/* Conteúdo (% / texto) é hard-coded no componente — clonar componente p/ variar */}
