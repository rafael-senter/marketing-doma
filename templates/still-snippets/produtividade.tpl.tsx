{/* ── PRODUTIVIDADE — {{NOME}} (plugin marketing-doma) ── */}
<Still id="prod-{{NOME}}" component={Produtividade270 as never}
  width={vertical.largura} height={vertical.altura} />
{/* Conteúdo (título + foto) é hard-coded no componente — clonar p/ variar.
    Variantes: Produtividade254 (operário nanobanana), Produtividade277 (selo). */}
