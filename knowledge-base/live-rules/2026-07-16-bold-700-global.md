# Negrito global = 700 (era 800, grosso demais)

**Data:** 2026-07-16
**Descoberto em:** POST prejuizo-industria (correção do Patrick: "precisa afinar o negrito — regra em todas as criações")
**Sintoma:** `TextoRico` renderizava `**bold**` com fontWeight 800 — mais grosso que o modelo em qualquer peça.
**Regra:** default do `boldWeight` no `TextoRico` (base de TODOS os componentes) = **700**. 800 só se medição do modelo específico pedir (passar `boldWeight={800}` explícito).
**Aplicar em:** todas as categorias, todas as criações novas. Recriações 1:1 antigas: re-validar com compare.py se forem re-renderizadas.
