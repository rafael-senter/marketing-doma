# Categoria nova "SEGMENTOS" (POST 234) — template + regra visão-do-dono

**Data:** 2026-07-16
**Descoberto em:** POST segmento-naturais (v1 + v2)
**Regra:** categoria "segmentos" criada (`templates/components/segmentos/Segmentos.tsx`): foto full-bleed TROCÁVEL (prop `foto`) + card manga topo-colado radius 100 só embaixo + wordmark DOMa soft width 100% top 0 + texto com `**bold**`/`__sublinhado__`. Ficha completa: `../padroes/segmentos.md`.
**Regra de imagem:** foto de segmento = "VISÃO DO DONO" — ambiente típico do negócio (plaquinhas R$/kg em PT, PDV, balança, kraft), NUNCA showroom genérico. Sem pessoas. Manter v1 (clean) e v2 (real) como opções.
**Aplicar em:** toda peça nova da categoria segmentos; regra visão-do-dono vale pra qualquer foto de ambiente de negócio.
