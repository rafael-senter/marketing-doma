---
name: render
description: "Atalho de render via render-still.sh (scale 2 → Lanczos, anti-franja)."
---

# `/marketing-doma:render <id...>` ou `<prefix> <N>`

**EXECUTAR DIRETO.**

```bash
bash ~/.claude/skills/marketing-doma/scripts/render-batch.sh "$@"
```

Aceita: ID único, lista, ou `<prefix> <N>` (range prefix-1..prefix-N).
